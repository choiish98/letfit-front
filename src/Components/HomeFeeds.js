import React, { useEffect, useState } from "react";
import { View, Text, KeyboardAvoidingView, AsyncStorage } from "react-native";
import {API_URL} from "@env";

const Login = (props) => {
    return (
        <Text>This is Home Feed</Text>
    );
};

export default Login;